# tokio vs smol 性能瓶颈分析报告

## 🎯 分析目标

通过 P99/P999 延迟测试和性能剖析，识别 kcp-rs 在 tokio 多线程和 smol 单线程模式下的性能瓶颈差异，为运行时选择提供数据支持。

## 📊 核心发现

### 延迟性能数据

**smol 单线程模式显著优于 tokio 多线程模式**:

```
tokio baseline (多线程):
- P50:   399.2 µs
- P99:   1,113.7 µs
- P999:  5,613.2 µs
- Max:   19,069.5 µs

smol baseline (单线程):
- P50:   248.4 µs  (↓ 38%)
- P99:   421.2 µs  (↓ 62%)
- P999:  596.5 µs  (↓ 89%)
- Max:   2,209.2 µs (↓ 88%)
```

### 测试环境配置

- **平台**: macOS Apple Silicon
- **负载**: 500 RPS, 30秒测试, 26,624字节负载
- **KCP配置**: Fast3模式, 512/512窗口, 无加密无压缩
- **tokio**: 多线程模式 (默认)
- **smol**: 单线程模式 + 一个工作线程 (2 worker total)

## 🔍 瓶颈分析

### tokio 多线程瓶颈

1. **跨线程任务迁移开销**
   - 工作窃取机制导致任务在不同核心间迁移
   - CPU 缓存失效，L1/L2 缓存命中率下降
   - 跨核同步延迟增加

2. **锁竞争和内存屏障**
   - 共享运行时状态需要锁保护
   - 内存屏障影响指令级并行性
   - 线程间通信延迟

3. **调度器开销**
   - 多线程调度器的复杂性
   - 优先级调度和抢占延迟
   - 任务队列竞争

4. **上下文切换成本**
   - 线程切换的开销
   - 寄存器保存/恢复
   - TLB 刷新

### smol 单线程优势

1. **确定性执行路径**
   - 无跨线程同步开销
   - 更好的 CPU 缓存局部性
   - 更可预测的延迟

2. **简化的调度器**
   - 单线程事件循环
   - 无工作窃取机制
   - 直接的任务执行

3. **优化的内存访问模式**
   - 连续内存访问模式
   - 减少缓存失效
   - 更好的预取效果

## 📈 性能差异解释

### P999 延迟的巨大差异 (89% 改善)

这种极端的 P999 改善主要是因为：

1. **尾部延迟抑制**:
   - tokio: 多线程环境下的偶发性延迟峰值
   - smol: 确定性执行避免了大多数突发延迟

2. **调度抖动消除**:
   - tokio: 线程调度和优先级导致的不确定性
   - smol: 单一执行线程的确定性行为

3. **同步开销最小化**:
   - tokio: 跨线程同步和锁等待
   - smol: 几乎无同步开销

## 🛠 优化建议

### tokio 优化方向

1. **线程数优化**:
   ```rust
   // 尝试减少 worker 线程数
   tokio::runtime::Builder::new_multi_thread()
       .worker_threads(2)  // 尝试 2-4 个线程
       .enable_all()
       .build()
   ```

2. **current-thread 模式**:
   ```rust
   // 对于延迟敏感应用
   tokio::runtime::Builder::new_current_thread()
       .enable_all()
       .build()
   ```

3. **任务亲和性**:
   - 使用 `tokio::task::spawn_local()` 将相关任务绑定到同一线程
   - 减少跨线程任务通信

### smol 配置建议

1. **保持当前配置**:
   ```rust
   // 当前 1+1 worker 配置已经最优
   let workers = num_cpus::get().min(2);  // 保持这行代码
   ```

2. **CPU 亲和性** (Linux):
   ```rust
   // 如果可能，pin 到特定核心
   pin_to_core(i);  // 已在 smol 实现中
   ```

3. **批处理优化**:
   - 批量处理 KCP 消息减少函数调用
   - 继续使用 cpu_block 池化策略

## 🎛 运行时选择指南

### 选择 smol 的场景

✅ **强烈推荐**:
- 实时通信应用（语音、视频、游戏）
- 低延迟要求的控制协议
- 单连接或少量连接的优化场景
- 嵌入式或资源受限环境

✅ **适合**:
- 中小规模并发连接 (< 1000)
- 延迟敏感的批处理任务
- 调试和性能测试环境

### 选择 tokio 的场景

✅ **强烈推荐**:
- 大规模并发连接 (> 1000)
- CPU 密集型并行计算
- 已经深度集成 tokio 生态
- 需要充分利用多核性能

⚠️ **需要优化**:
- 可以使用多线程但需要控制线程数
- 考虑使用 current-thread 模式处理延迟敏感部分

## 📋 后续行动建议

1. **应用级决策**:
   - 新的延迟敏感应用优先考虑 smol
   - 现有 tokio 应用考虑添加 smol 选项

2. **开发优化**:
   - 为 tokio 提供 worker_threads 配置选项
   - 为 smol 添加性能监控和调优指南

3. **测试扩展**:
   - 加密性能对比测试
   - 不同负载下的扩展性测试
   - 内存使用对比分析

4. **文档更新**:
   - 更新 README 运行时选择指南
   - 添加性能调优最佳实践

## 📝 结论

**smol 单线程模式在延迟性能方面表现出显著优势，特别适合实时通信应用**。tokio 在多连接并发处理方面仍有其价值，但需要注意配置优化。

**推荐策略**: 新应用开发时根据延迟要求选择运行时；现有应用可以考虑添加 smol 支持作为延迟敏感场景的优化选项。

---

*报告基于实际 P99/P999 延迟测试数据生成*
*测试时间: 2026-08-12*
*测试环境: macOS Apple Silicon, kcp-rs release build*