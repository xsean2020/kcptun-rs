# 高速 **KCP/UDP** 无锁异步网络库架构设计与落地 文档 

##### **V4 ·** 高性能实现修订版（低 **P99 / Worker** 绑核 **/ 批量 UDP /** 正确 **AsyncStream** 语义） 

定位：在保留 Tokio async/await 使用体验的前提下，将 KCP 数据面设计成单所有者、少跨核、少系统调用、少分配、 可控背压的高性能网络引擎。本文基于 V3 重新审查并补全正确性、性能上限、生命周期、定时器、内存、内核网络栈、 观测与压测设计。 

## **0. V3** 审查结论：先修正确性，再谈无锁 

V3 的核心方向——控制面与数据面隔离、Session Sticky Worker、Run-to-Completion、Double-Check Waker——是 正确的。但若直接按文档代码落地，会在队列拓扑、AsyncWrite 语义、Session 关闭、内存规模和 UDP I/O 路径上遇到 明显问题。 

|问题|**V3**风险|**V4修正**|
|---|---|---|
|WorkerCommand<br>使用SPSC|多个connect/task<br>直接向一个<br>Worker<br>送命令，本是<br>MPSC；<br>SPSC Producer不能多生产者共享|增加单一Dispatcher<br>作为唯一<br>Producer<br>，或控制面改用有界<br>MPSC；hot packet path<br>仍保持<br>SPSC|
|AcceptQueue<br>使用SPSC|多个Worker<br>同时产出accepted<br>stream<br>时不是SPSC|每Worker<br>一条accept SPSC<br>，由<br>Listener<br>聚合；或专用Acceptor作<br>为单Producer|
|三级RX<br>Worker<br>TX<br>→<br>→|每包<br>次跨核<br>两<br>ownership transfer，<br>P99<br>可能被cache bouncing放大|优先采用Worker<br>自己收发UDP；<br>portable fallback<br>才使用RX<br>dispatcher|
|每Session 2×64KB byte ring|10<br>万连接约12.8GB<br>，仅ring即不可<br>接受|默认小ring +<br>全局slab/chunk pool<br>+<br>动态水位；区分控制面和payload<br>内存|
|poll_fush<br>永远Ready|并不代表KCP已接收应用数据、更不<br>代表UDP已提交|维护tx sequence / accepted<br>watermark，fush<br>等待Worker<br>drain<br>到KCP|
|poll_shutdown<br>立即Ready|Close<br>命令可能队列满，且没有<br>graceful/abort<br>分<br>区|Close state machine + close<br>ack/timeout + EOF/error原子状态|
|Worker<br>全Session扫描|连接数上升后O(N)空扫，CPU与<br>P99恶化|按kcp.check()/next_update<br>建min-<br>heap<br>或timing wheel<br>，只驱动到期<br>Session|
|每push/pop<br>都wake|高负载产生wake storm|仅状态边沿wake：empty<br>non-<br>→<br>empty、full<br>writable<br>→<br>、close/error|
|conv随机即身份|碰撞、伪包、地址迁移、反射/DDoS<br>均未定义|SessionKey + token/cookie + peer<br>validation + handshake rate limit|



## **1.** 性能目标与设计优先级 

高性能网络库的核心不是“尽可能无锁”，而是让数据的所有权尽量长期停留在同一个 CPU 上。一次 lock-free queue push 仍可能触发跨核 cache-line 移；因此先整如下： 

KCP/UDP Architecture V4 · High Performance Implementation 

- 第一先：Session 与其 KCP 状态、timer、socket I/O 尽可能由同一 Worker/CPU 独占。 

- 第二优先级：减少每包系统调用，RX/TX 批理； Linux 下可选 recvmmsg/sendmmsg、UDP GSO/GRO 或 io_uring multishot。 

- 第三优先级：避免 per-packet heap allocation 与 memcpy；使用固定 PacketSlab/ChunkPool、descriptor ring。 

- 第四优先级：跨线程通道必须有明确生产者数量；payload hot path 首选 SPSC。 

- 第五优先级：减少无意义 wake、timer scan、hash、atomic、clock_gettime。 

- 第六优先级：控制面可以适度使用 MPSC/Mutex，只要不进入 packet hot path。 

## **2.** 推荐总体架构：两档实现 

### **2.1 A** 档： **Portable High-Performance** （推荐先落地） 

┌──────────── Tokio Control Plane ────────────┐ KcpStream ─────►│ app_tx SPSC / app_rx SPSC / AtomicWaker    │ └────────────────┬─────────────────────────────┘ per-session SPSC│ ▼ UDP Socket ─► RX Dispatcher ─► Worker[0..N-1] ─► TX Batch Thread ─► NIC │ │ packet_ref SPSC  │ ├─ SessionMap (owned) └──────────────────►├─ KCP engine └─ Timer wheel / deadline heap 

优点：实现简单、跨平台、易测试。缺点：每个 packet 至少经过 dispatcher→worker，发送侧若独立 TX thread 又增 加一次跨核传递。适用于先验证 API 与 KCP correctness。 

### **2.2 S** 档： **Linux Low-P99** （最终性能目标） 

NIC/RSS queues │ ▼ ┌──────────────┐   SO_REUSEPORT / reuseport-BPF / queue affinity UDP socket 0  │ │─────► Worker 0 / Core C0 ── recv batch  KCP  send batch─ ─ ├──────────────┤ UDP socket 1  │ │─────► Worker 1 / Core C1 ── recv batch  KCP  send batch─ ─ ├──────────────┤ ...          │ │ └──────────────┘ 

Tokio tasks  ◄──── per-session SPSC descriptors + edge-triggered waker ────► Worker 该模式消除专用 RX/TX 线程，使 UDP receive、KCP、UDP send 都由 Session 所属 Worker 完成。真正需要解决的 是入包如何“ 稳定路由到同一 Worker”。可以按 4-tuple/RSS，或 Linux SO_REUSEPORT + BPF 按协议头中的 stable session key 选择 socket。新连接在握手阶段需要特殊分发策略。 

KCP/UDP Architecture V4 · High Performance Implementation 

## **3.** 线程模型与 **CPU** 亲和性 

### **3.1 Worker = OS Thread，不是 Tokio Task** 

每个 Worker 是长期运行的 native thread，独占其 SessionMap、timer queue、packet batch、KCP state。Worker 内不 await，不 spawn，不持有跨 Worker session lock。 

### **3.2** 绑核原则 

- Worker 与 NIC RX queue 尽量同 NUMA node；不要只固定“ CPU”，还要关注 IRQ/RSS queue 所在 CPU。 

- Linux S 档建议：IRQ/RSS queue → Worker CPU 一一或少量映射；TX 结合 XPS 做同核/邻核发送。  不要把 Tokio runtime worker 与数据面 Worker 绑到同一组核心；否则应用突发会直接污染网络数据面 P99。 

- 保留 1~2 个 CPU 给内核 softirq/系统线程，避免所有核全部 pin 满。 

- 跨 NUMA 时 PacketPool、Session arena、socket buffer 都应本地分配；不要用一个全局共享 pool 热点。 

## **4.** 队列拓扑：必须先证明 **SPSC** 

### **4.1 Payload 通道** 

每个 KcpStream 与所属 Worker 之间天然只有一个 producer 和一个 consumer，因此最适合 SPSC： 

|**方向**|**Producer**|**Consumer**|推荐|
|---|---|---|---|
|App<br>Worker<br>→|该KcpStream<br>的<br>AsyncWrite侧|唯一Worker|SPSC byte/chunk<br>descriptor ring|
|Worker<br>App<br>→|唯一Worker|该KcpStream<br>的<br>AsyncRead侧|SPSC byte/chunk<br>descriptor ring|
|RX Dispatcher<br>Worker<br>→|唯一RX dispatcher|唯一Worker|每Worker<br>一条SPSC<br>PacketRef ring|
|Worker<br>TX<br>→|唯一Worker|唯一TX aggregator|每Worker<br>一条SPSC<br>PacketRef ring|



### **4.2 Control Command 不是天然 SPSC** 

connect/close/config 等命令可能来自多个 Tokio task。不要为了全无锁强行让“ ” rtrb Producer 多生产者共享。推荐两 方式： 1. 高吞吐推荐：ControlDispatcher 使用一个有界 MPSC 接收控制命令，再由 Dispatcher 作为唯一 Producer 入每 Worker 的 SPSC command ring。 

2. 低度推：Worker command 使用有界 MPSC（crossbeam/channel 或自研 MPSC），因为控制命令频率远低于 packet path。 

原则：控制面上的一把锁或 MPSC 通常比破坏 SPSC 语义更便宜、更正确。 

## **5. Packet** 内存模型：不要让 **byte ring** 吃掉几 **GB** 

### **5.1 V3 固定 64KB×2** 的问题 

若 100,000 Session 每个方向 64KB，仅应用桥接 ring 就约 12.8GB，还未包括 KCP send/recv queue、socket buffer、 HashMap、任务和业务内存。高并发下应将“容量”与“已占用物理 payload 内存”解耦。 

KCP/UDP Architecture V4 · High Performance Implementation 

### **5.2** 推荐： **Descriptor Ring + Slab/ChunkPool** 

struct ChunkRef { slab_id: u32, offset: u16, len: u16, flags: u16, } 

Per-session SPSC: Ring<ChunkRef>      // 小、cache-friendly Per-worker PacketSlab: [MTU chunk]   // local pool, no cross-NUMA free Large application writes: segmented into 4K/16K chunks or copied directly to KCP staging 对于 KCP，网络 packet 通常 MTU 级，建议 UDP PacketSlab 按 1536/2048 bytes 固定块；应用 stream 可采用 4KB/16KB chunk。descriptor ring 容量可 32~256 ，而不是每接永久留 128KB payload。 

### **5.3 Buffer ownership** 

- RX：Worker/dispatcher 从 pool 取 buffer → recv into buffer → PacketRef 入 ring → Worker input KCP → buffer 归还原 pool。 

- TX：KCP output 直接写入 pool buffer → batch send → completion 后归还。 

- 跨 NUMA 归还不要写全局 freelist；使用 owner-worker recycle ring，避免远端 free cache bouncing。 

## **6. UDP RX/TX** ：批量比异步本身更重要 **“ ”** 

### **6.1 Portable baseline** 

Worker socket 使用 nonblocking recv_from/send_to，循环 drain 到 EAGAIN。一次唤醒尽可能处理一批 packet，设 置 packet/time budget 防止某 Worker 长时间霸占 CPU。 

### **6.2 Linux fast path** 

- recvmmsg/sendmmsg：一次 syscall 收/发多个 datagram，降低 syscall 与用户态/内核态切换成本。 

- UDP GSO（UDP_SEGMENT）：应用提交较大 buffer，让内核/NIC 切成多个 UDP datagram；适合连续 KCP segment 输出可合并时。 

- UDP GRO：接收侧可将多个 datagram 聚合上送，但应用必须正确解析 segment size；应做能力探测与 fallback。 

- io_uring multishot recv：可减少重复提交 receive request；需要严格管理 buffer ring 生命周期，不建议作为 V4 第一版唯一实现。 

- 上建先做 recvmmsg/sendmmsg backend，再将 io_uring 作为 feature-gated backend。不要把 io_uring 与必然更“ 快绑定；小包” UDP 下 CPU topology、batch size、buffer ownership 往往更定 P99。 

## **7. KCP Session** 调度：严禁每轮扫描全部连接 

### **7.1 Deadline-driven** 

每个 Session 记录 next_update_ms。调用 kcp.update(now) 后再使用 kcp.check(now) 计算下一 deadline。Worker 只处理 deadline 到期的 Session。 

struct SessionMeta { 

next_update_ms: u32, 

timer_generation: u32, 

KCP/UDP Architecture V4 · High Performance Implementation 

// ... 

} 

loop { 

now = clock.cached_ms(); 

drain_control_budget(); recv_udp_batch_budget(); drive_app_tx_budget(); 

while timer.peek_deadline() <= now && timer_budget_not_exhausted() { id = timer.pop(); 

s = sessions.get_mut(id); 

s.kcp.update(now); drain_kcp_output(s); deadline = s.kcp.check(now); timer.insert(id, deadline); 

} 

flush_udp_tx_batch(); 

adaptive_idle(next_deadline); } 

### **7.2 Heap 是 Timing Wheel** 

|**方案**|**适用**|优缺点|
|---|---|---|
|Binary min-heap|1~5万活跃连接、deadline分散|；<br>O(logN)，lazy generation可<br>避免decrease-key|
|Hierarchical timing wheel|10万+连接、KCP interval 10~20ms|O(1)摊销、cache更稳定；实现稍复<br>杂|
|全量<br>描<br>扫|仅小规模测试|禁止用于正式高并发路径|



KCP update interval 常见为毫秒级，不需要纳秒时钟。Worker 每 loop 读取一次 monotonic time，并在该批处理中复 用，减少 clock_gettime 调用。 

## **8. AsyncRead / AsyncWrite** 正确实现 

### **8.1** 必须补充 **StreamState** 

#[repr(u8)] 

enum ConnState { Connecting, Established, PeerClosed, Closing, Closed, Error } 

struct SharedIoState { 

state: AtomicU8, error: AtomicI32, rx_waiting: AtomicBool, tx_waiting: AtomicBool, flush_waiting: AtomicBool, 

KCP/UDP Architecture V4 · High Performance Implementation 

rx_waker: AtomicWaker, tx_waker: AtomicWaker, flush_waker: AtomicWaker, tx_submitted: AtomicU64, tx_consumed: AtomicU64, 

} 

### **8.2 poll_read** 

序：取 ring → 若有数据 Ready；若 state 表示 PeerClosed/Closed 且 ring 已空，返回 EOF（Ok 且 0 bytes）；若 Error 返回 error；否则设置 waiting/register waker → double-check 数据与 state → Pending。 

### **8.3 poll_write** 

只保证应用数据成功进入“ app→worker queue”，不保证已发到 UDP。若连接已 Closing/Closed/Error，应返回 。 BrokenPipe/对应错误。支持 partial write 

### **8.4 poll_flush** 

至少应定义为“Worker 已将 flush 调用前写入的数据从 app_tx queue 消费并提交给 KCP”。实现：记录 tx_submitted sequence，Worker drain 后更新 tx_consumed；flush 等待 consumed ≥ target。若要定义成对端已“ ACK”，则需要 额外 KCP snd_buf/snd_queue watermark，代价更高，不建议模拟 std::TCP 的语义。 

### **8.5 poll_shutdown** 

shutdown 是状态机，不是 push Close 后立刻成功。至少支持 Abort 与 Graceful ：两种Graceful 先 flush app_tx，再发送 协协 close/等待有限时间；Abort 立即删除 Session。命令队列满时必须注册可写通知或走可靠控制面，不能静默丢 Close。 

**9. Waker** ：从每次 **“ wake”** 改成边沿通知 V3 每次 Worker push app_rx 或 pop app_tx 都 wake，会造成 wake storm。推荐等待标志 + 状态边沿： 

// App poll_read: queue empty 

rx_waiting.store(true, Release); 

rx_waker.register(cx.waker()); 

if !rx_queue.is_empty() || terminal_state() { 

rx_waiting.store(false, Relaxed); 

return Ready(...); 

} 

return Pending; 

// Worker: empty -> non-empty / close / error 

if rx_waiting.swap(false, AcqRel) { 

rx_waker.wake(); 

} 同理 tx 仅在之前“ full 且 Writer Pending，现在变为 writable”时 wake；flush 仅 watermark 越过目标时 wake。 AtomicWaker 的 register + double-check 仍保留。 

KCP/UDP Architecture V4 · High Performance Implementation 

## **10. App→KCP** 路径：减少临时 **2KB copy** 

V3 使用 [u8; 2048] temp buffer 先从 app ring pop，再 kcp.send；这会让较大 write 被反复切成 2KB，并多一次 copy。建议： 

- 若 ring 支持 contiguous slice/peek，则直接将连续片段传给 KCP send，再 advance consumer。 

- 若需要 stream coalescing，使用 Worker-local 8~32KB staging buffer，按 KCP MSS/业务 flush 策略批量送入。 

- 避免每个 Session 都永久拥有 staging buffer；从 per-worker scratch arena 临时借用。 

- 对小 write 可 delayed-coalesce（例如同一 worker tick 合并），但要提供 low_latency=true 关闭聚合。 

## **11. KCP output** ：批量发送与 **pacing** 

KCP output callback 不应直接每 segment 调一次 sendto。output callback 只把 datagram descriptor 放入 Workerlocal TxBatch；当前 loop 末尾统一 sendmmsg/io_uring submit。 

fn kcp_output(segment: &[u8], peer: SocketAddr, ctx: &mut WorkerCtx) { 

let pkt = ctx.tx_pool.alloc(); 

pkt.copy_from_slice(segment); 

ctx.tx_batch.push(TxDesc { pkt, peer }); 

if ctx.tx_batch.is_full() { ctx.flush_tx(); } 

} 

同时设置每 Session/Worker send budget，防止一个高丢包 Session 的重传占满整个 worker。可按 bytes-per-loop 或 datagrams-per-loop 做 fairness。 

## **12. SessionMap** 与 **SessionKey** 

### **12.1 不** 应 **只使用 conv:u32** 

外部 UDP 包可伪造 conv，随机 32bit 也存在碰撞。至少使用 (conv, peer endpoint) 作为 lookup key；若允许 NAT rebinding，则使用 server-issued 64/96-bit token 对 endpoint 变更做认证。 

### **12.2 Map** 选择 

Session 由单 Worker 独占，可使用 hashbrown::HashMap。若 conv 已由服务端随机且攻击面可控，可选择更快 hasher；若直接接受不可信 key，不能使用容易构造 collision 的无防护 hash。更进一步可用 dense SessionId→slab index，将网络 key map 与 Session storage 分离，提高迭代/timer cache locality。 

## **13.** 建连协议必须补齐 

KCP 本身不是完整的安全连接建立协议。服务端 Listener 需要定义至少以下步骤： 

- SYN/HELLO：客户端带随机 nonce，不立即分配大 Session。 

- Cookie/Challenge：服务端基于 peer+nonce+secret 生成无状态 cookie，限制 spoofed source 放大攻击。 

- CONNECT：客户端回 cookie；服务端此时分配 conv/session token 与 Worker。  ACK/ESTABLISHED：客户端确认后 KcpStream connect() 才 Ready。  Rate limit：按 IP/prefix/global 对半连接、cookie 验证失败、未知 conv 做令牌桶。  Anti-amplification：未验证地址前，服务端响应字节数必须受限。 

KCP/UDP Architecture V4 · High Performance Implementation 

### **13.1 Worker** 选择 

连接建立后 sticky，不做在线迁移。选择算法优先使用 power-of-two-choices（从 hash 选两个 Worker，比较 load_score），通常比扫描所有“ Worker 找最小 active_sessions”更便宜且可扩展。load_score 不只看连接数，应包含 active_sessions、tx_pending_bytes、timer_lag、rx_queue_depth。 

## **14. Listener accept 聚合** 

推荐每 Worker 一条 accepted-session SPSC：Worker 是 producer，ListenerAggregator 是 consumer。Aggregator 再把 Stream 放入单消费者 async accept queue，或 Listener poll 时 round-robin 检查 N 个 worker accept ring。 若 Worker 数 4~16，poll_accept 扫描 N 条小 ring 的成本通常可接受；若 Worker 数很大，可维护一个 ready-worker bitmap，并由 worker 在 empty→non-empty 时原子置位 + wake listener。 

## **15. Backpressure** ：三层水位必须区分 

|层级|含义|满时策略|
|---|---|---|
|AppTXqueue|Tokio task写入过快|poll_write Pending；edge wake|
|KCP snd_queue/snd_buf|网络/<br>对端ACK变慢|停止从App TX drain；<br>背<br>自然上<br>让<br>压<br>传|
|UDP TX batch/socket|内核sndbuf/EAGAIN|保留待<br>送<br>发<br>descriptor<br>；注册<br>POLLOUT或下<br>重<br>；有硬上限<br>轮<br>试|
|App RX queue|应用消费慢|暂停kcp.recv drain<br>；<br>到<br>达<br>hard<br>limit可断开慢消费者|



不能在 app_rx ring 满时直接丢 KCP 已重组出的可靠数据，否则会破坏可靠语义。正确策略是停止从 KCP recv queue 取数据，让 KCP 接收窗口/内部队列形成背压；必要时设置最大应用滞留并主动关闭。 

## **16. Worker Event Loop** ：预算化 **Run-to-Completion** 

const RX_BUDGET: usize = 64; const CMD_BUDGET: usize = 128; const APP_TX_BUDGET: usize = 256; const TIMER_BUDGET: usize = 512; const TX_BATCH: usize = 64; 

###### loop { 

let now = clock.now_ms_cached(); let mut activity = 0; 

activity += recv_batch(RX_BUDGET); 

activity += drain_commands(CMD_BUDGET); activity += drain_app_tx(APP_TX_BUDGET); activity += run_due_timers(now, TIMER_BUDGET); activity += flush_tx_batch(TX_BATCH); activity += drain_kcp_rx_to_app(APP_TX_BUDGET); 

recycle_packets(); 

KCP/UDP Architecture V4 · High Performance Implementation 

if activity == 0 { idle_wait(next_timer_deadline()); 

} 

} 所有阶段均有 budget，防止 RX flood、某个 session 重或 control burst 饿死其它阶段。若 budget 命中上限，下一轮立 。 即继续，不进入 park 

## **17. Adaptive Idle** ：基于 **deadline** ，不要固定 **park 500µs** 

- 固定 park_timeout(500µs) 会直接给空闲连接引入最高约数百微秒额外唤醒延迟。更合理的策略： 

3. 近期持续活跃：spin 20~100 次（按 CPU/测量调优，而不是固定 1000/2000 次）。 

4. 短空：yield 或 cpu_relax + 极短 timeout。 

5. 长期空闲：park 到 min(next_kcp_deadline-now, max_idle_sleep)，且 command/IO notifier 可以立即 unpark。 

6. Linux socket backend：阻塞 poll/epoll/io_uring 等待 socket readiness + eventfd/control notifier + timer deadline，低负载时 CPU 可接近 0。 

客户端低连接数尤其适合 event-driven idle；服务端高流量 worker 则通常持续处于 active loop。 

## **18. Linux Socket /** 内核网络栈优化清单 

|项目|建议|说明|
|---|---|---|
|SO_REUSEPORT|S档开启|多worker socket<br>同端口；可配<br>reuseport-BPF做<br>定分流<br>稳|
|SO_RCVBUF/SO_SNDBUF|压测后设置|过小丢包，过大掩盖拥塞并增加内<br>存；记录实际 getsockopt值|
|RSS/RPS/RFS/XPS|按部署调优|让NIC queue、softirq、worker<br>CPU拓<br>一致，避免<br>跳核<br>扑<br>乱|
|recvmmsg/sendmmsg|优先实现|低风险、收益稳定的batch syscall<br>backend|
|UDP_SEGMENT/GSO|feature probe|TX合批；硬件/<br>内核不支持则<br>fallback|
|UDP_GRO|featureprobe|RX<br>聚合后需按segment size拆分|
|SO_BUSY_POLL|只对极低延迟场景A/B|会明显增加CPU，必须通过<br>p99/CPU数据决定<br>|
|io_uring|后续backend|multishot recv/bufer ring有潜力，<br>但复杂度高|



## **19. CPU / NUMA / Cache Layout** 

- Session hot fields（kcp pointer/state、peer、next_timer、queue indices）放在前部；统计计数器等 cold fields 

   - 分离，减少 cache footprint。 

- Worker metrics 使用 per-worker counters，采集线程周期汇总；不要每包写共享 AtomicU64。 

- 频繁更新的 producer/consumer index 应 cache-line padding，避免 false sharing。 

- PacketPool/TimerWheel/Session slab per-worker；全局只保存 immutable routing snapshot。 

- WorkerRef 的 active score 可低频更新成 cache-aligned atomic snapshot，路由不必实时读取大量共享原子。 

KCP/UDP Architecture V4 · High Performance Implementation 

## **20. Session** 生命周期状态机 

CONNECTING 

handshake ok│ ▼ 

ESTABLISHED ── peer close ─► PEER_CLOSED ─► CLOSED │ 

├─ local graceful close ─► DRAINING ─► CLOSING ─► CLOSED 

│ ├─ local abort ────────────────────────────────► CLOSED │ 

└─ timeout/protocol/io error ─────────────────► ERROR 

SessionDropGuard 不能只做 active_sessions--。Drop KcpStream 时应发送 best-effort abort/close command；若控 制命令可靠性不能保证，需要 Worker 通过 shared state 的 owner-dead flag 或 generation table 定期回收 orphan session。所有终态都必须 wake rx/tx/flush/connect waker。 

## **21.** 超时与异常场景 

|场景|必须定义的行为|
|---|---|
|未知convpacket|快速drop<br>，不分配；计数+ rate limit|
|重复/<br>乱序handshake|幂等处理，cookie/token验证|
|peer地址改变|默认拒绝；启用rebinding<br>时要求token验证|
|AppRX长期不读|窗口/queue<br>背压，超过hard timeout断开|
|AppTX长期满|poll_write Pending<br>；可配置write timeout在上<br>理<br>层处|
|Worker panic|Supervisor<br>标记该worker sessions error<br>并wake所有<br>task；不得<br>默<br>起<br>静<br>挂|
|命令队列满|连接创建返回WouldBlock/Busy<br>或await queue<br>capacity；Close使用可靠路径|
|UDP EMSGSIZE|按path MTU/MSS降级；禁止无限重试|
|socket ENOBUFS/EAGAIN|计数并短期重试；持续异常触发overloadpolicy|
|clock wrap|KCP u32 ms<br>使用wrapping compare；timer实现必须<br>兼容wrap-around|



## **22.** 配置结构建议 

pub struct KcpEngineConfig { pub workers: usize, pub worker_cpu_set: Vec<usize>, pub io_backend: IoBackend,      // Portable | Mmsg | IoUring 

pub topology: IoTopology,       // Dispatcher | ReusePortPerWorker 

pub mtu: usize, pub kcp_interval_ms: u32, pub nodelay: bool, pub fast_resend: i32, pub nocwnd: bool, pub snd_wnd: u16, 

KCP/UDP Architecture V4 · High Performance Implementation 

pub rcv_wnd: u16, 

pub app_tx_slots: usize, pub app_rx_slots: usize, pub packet_pool_per_worker: usize, pub rx_batch: usize, pub tx_batch: usize, 

pub max_sessions_per_worker: usize, pub max_pending_app_rx_bytes: usize, pub max_pending_app_tx_bytes: usize, pub idle_timeout: Duration, pub handshake_timeout: Duration, 

pub socket_rcvbuf: usize, pub socket_sndbuf: usize, pub reuse_port: bool, pub busy_poll_us: Option<u32>, } 配置必须把“低延迟”和“高吞吐”解耦。例如 low_latency profile：小 batch、无 delayed coalescing、短 spin； throughput profile：较大 batch、允许 GSO/coalesce。 

## **23.** 推荐核心数据结构 

struct Worker { id: WorkerId, socket: UdpBackend, sessions: SessionTable, deadlines: TimerWheel, cmd_rx: ControlRx, tx_batch: TxBatch, packet_pool: PacketPool, recycle_rx: RecycleRing, metrics: WorkerMetrics, } 

struct Session { 

id: SessionId, key: SessionKey, peer: SocketAddr, kcp: Kcp, shared: Arc<SharedIoState>, app_rx_prod: SpscProducer<ChunkRef>, app_tx_cons: SpscConsumer<ChunkRef>, next_update_ms: u32, last_rx_ms: u32, last_tx_ms: u32, 

KCP/UDP Architecture V4 · High Performance Implementation 

pending_flags: u32, } 

## **24.** 路由策略：从一致性哈希改为“稳定候选 **+** 负载分数” 

Worker 是进程内固定集合时，一致性哈希的“节点增删最小迁移”优势并不大，因为 Session 本身禁止迁移。连接建立只 需快速选一次 Worker。建议： 

let h = hash(session_seed); 

let a = h as usize % workers.len(); let b = mix(h) as usize % workers.len(); let wa = workers[a].load_score.load(Relaxed); 

let wb = workers[b].load_score.load(Relaxed); 

let target = if wa <= wb { a } else { b }; 

Power-of-two choices 是 O(1)，不需要扫描所有 worker；比固定阈值 >16 更合理。load_score 可每 10~100ms 由 Worker 发布一次，避免 connect hot path 连续读取多个易变原子。 

## **25. KcpStream API** 兼容层建议 

对上层仍实现 tokio::io::AsyncRead/AsyncWrite，但不要承诺与 TcpStream 的全部内核语义完全相同。建议额外暴露： 

- KcpStream::flush_to_kcp()：明确等待应用写队列被 Worker/KCP 接收。 

- KcpStream::close_graceful(timeout) / abort()。 

- KcpStream::stats_snapshot()：RTT、RTO、retrans、snd_queue、snd_buf、app queue 水位。 

- KcpStream::peer_addr() / session_id()。 

- KcpStream::set_priority()：可选，用于 Worker fairness/budget。 

## **26.** 可观测性：不测这些就无法调 **P99** 

|指标组|**至少采集**|
|---|---|
|UDP RX|packets, bytes, EAGAIN, ENOBUFS, kernel drops,<br>batch size|
|UDP TX|packets, bytes, EAGAIN/ENOBUFS, batch size, GSO<br>usage|
|Worker|loop/s, busy ratio, park time, timer lag, rx/cmd/app<br>budgets hit|
|Queue|app_rx/app_tx depth p50/p99/max, packet ring<br>drops/near-full|
|KCP|srtt/rto, retrans, fast-retrans, snd_queue, snd_buf,<br>recv_queue|
|Wake|rx_wake/s,tx_wake/s,spurious wake estimate|
|Memory|packet pool in-use/high-water, sessions, chunk<br>allocation failures|
|Latency|ingress<br>kcp.input, input<br>app wake, app<br>→<br>→<br>write<br>kcp.send,kcpoutput<br>send syscall<br>→<br>→|



所有指标优先 per-worker 非原子累加，1s 周期汇总。trace/span 不应默认进入每 packet hot path。 

KCP/UDP Architecture V4 · High Performance Implementation 

## **27.** 压测方法：吞吐和尾延迟分开看 

- 基线 1：单 Worker / 单 core，排除跨核因素，测协议本体上限。 

- 基线 2：N Worker，逐步 1→→→2 4 8，观察吞吐是否线性、P99 是否恶化。 

- 模型：64B/256B/1KB/MTU；低 PPS + burst；稳定 50/70/90% CPU。 

- 连接模型：100、1K、10K、100K mostly-idle；每档测 timer CPU。 

- 故障模型：1/5/10% loss、reorder、jitter；关注 retrans 对 Worker fairness 的影响。 

- 应用慢消费者：人为暂停 read，验证 app_rx backpressure 不丢可靠数据。 

- 比较：Tokio UDP baseline、smol baseline、V3 dispatcher、V4 mmsg、V4 reuseport。 

- 每组至少记录 p50/p90/p99/p999/max、CPU cycles/packet、context switches、cache-misses（perf）。 

## **28.** 落地顺序（推荐） 

7. 修复 API correctness：EOF/error/flush/shutdown/connect state machine。 

8. 重构控制命令拓扑，保证所有 SPSC 都是真正单生产者/单消费者。 

9. 引入 per-worker Session ownership + deadline scheduler，去掉全量 Session scan。 

10. 将 payload byte ring 改为 descriptor + chunk/slab pool，并做内存高水位统计。 

11. 实现 Worker-local TX batch，Linux backend 加 recvmmsg/sendmmsg。 

12. 实现 edge-triggered waker，统计 wake 次数并与消息数比对。 

13. 补齐 handshake/cookie/token、unknown-conv rate limit、timeout。 

14. 做 CPU affinity + RSS/XPS/NUMA 压测，确定生产 core topology。 

15. 如果 dispatcher 仍是瓶颈，再上 SO_REUSEPORT per-worker socket/BPF routing。 16. 最后评估 io_uring multishot/GSO/GRO，必须以 benchmark 证明收益后启用。 

## **29.** 最终推荐架构 



<!-- Start of picture text -->
┌──────────── Tokio Runtime / App ────────────┐<br>                     AsyncRead / AsyncWrite / connect / accept   │ │<br>└───────┬───────────────────────────┬──────────┘<br>                             per-session SPSC           control MPSC│ │<br>                             + edge waker              │ ▼<br>                                                Control Dispatcher│<br>                                                        per-worker SPSC│ │<br>▼ ▼<br>┌──────────────────────────────────────────────┐<br> NIC queue 0 ───►│  Worker 0 / CPU0                             │<br>                   UDP recv batch -> KCP -> app RX             │ │<br>                   app TX -> KCP -> UDP TX batch               │ │<br>                   SessionTable + TimerWheel + Local PacketPool │ │<br>├──────────────────────────────────────────────┤<br> NIC queue 1 ───►│  Worker 1 / CPU1                             │<br>├──────────────────────────────────────────────┤<br>                   ...                                          │ │<br>└──────────────────────────────────────────────┘<br><!-- End of picture text -->

Linux ultimate: SO_REUSEPORT / RSS affinity / optional BPF stable routing 

Portable fallback: one RX Dispatcher + per-worker SPSC PacketRef 

KCP/UDP Architecture V4 · High Performance Implementation 

一句话总结：V4 的核心不是“更多无锁结构”，而是“更少跨核、更少 copy、更少 syscall、更少 wake、更少 timer 空扫，同时保证每个并发原语的生产者/消费者关系真实成立”。 

## **30. V3 → V4** 关键改动清单 

|**V3**|**V4**|
|---|---|
|RX Thread<br>Worker<br>TX Thread<br>→<br>→<br>固定三级|S<br>档Worker自己收发；portable<br>才dispatcher|
|所有跨线程都追求SPSC|payload SPSC；control<br>可MPSC/dispatcher|
|每Session 2×64KB byte ring|小descriptor ring+per-worker chunk/slabpool|
|temp [u8;2048] 中转|contiguous slice/staging /chunk ref|
|所有Session<br>每loopdrive|deadline heap/timingwheel<br>|
|每<br>据<br>数<br>push/popwake<br>|edge-triggered waitingfag+ wake|
|fush()<br>立即Ready|sequence watermark|
|shutdown()<br>立即Ready|graceful/abort state machine|
|conv u32<br>即session identity|SessionKey/token/peer validation|
|active_sessions > 16才分流|power-of-two +periodic load_score<br>|
|固定spin/yield/park 500µs|deadline-aware idle + socket/control notifer<br>|
|缺少DDoS/half-open设计<br>|cookie/challenge/rate-limit/anti-amplifcation|
|缺少<br>核<br>内<br>batch/ofoad|mmsg+ optional GSO/GRO/io_uring|



##### 推荐的抽象 

#### 不要做： 

```
ServerWorkerPool
ClientWorkerPool
```

#### 而是： 

```
                  WorkerPool
                      │
        ┌─────────────┴─────────────┐
        │                           │
   Server Policy               Client Policy
        │                           │
        ▼                           ▼
 Worker / Session / KCP       Worker / Session / KCP
        │                           │
   UDP Backend                  UDP Backend
```

核心 `Worker` 可以完全共享： 

```
struct Worker {
    id: WorkerId,
```

```
    sessions: SessionTable,
    timer: TimerWheel,
    packet_pool: PacketPool,
```

```
    udp: UdpBackend,
```

```
    cmd_rx: CommandReceiver,
    metrics: WorkerMetrics,
```

KCP/UDP Architecture V4 · High Performance Implementation 

```
}
```

`WorkerPool` 也共享： 

```
struct WorkerPool<P: WorkerPolicy> {
    workers: Vec<WorkerHandle>,
    policy: P,
}
```

#### 真正不同的是 **Policy** 。 

|行为|**Server**|**Client**|
|---|---|---|
|Worker数量|通常固定，<br>≈NIC/RSS/CPU|动态或较少|
|CPU afnity|强烈建|可选/按负载|
|UDP Socket|per-worker socket最佳|可共享或 per-worker|
|Session建|UDP packet/handshake驱<br>动|`connect()` 主建|
|Worker选择|packet hash / RSS /<br>reuseport|least-load / hash|
|Worker扩缩<br>容|尽量不动态|可以动态|
|Idle策略|Spin为主|Spin<br>Yield<br>Park<br>→<br>→|
|Session迁移|最好禁止|原则上也禁止|
|Accept|有|无|
|Connect|无|有|



#### 所以可以抽象成： 

```
trait WorkerPolicy: Send + Sync + 'static {
    fn select_worker(
        &self,
        ctx: &SelectContext,
        workers: &[WorkerHandle],
    ) -> WorkerId;
```

```
    fn idle_strategy(&self) -> IdleStrategy;
```

```
    fn allow_scale_up(&self) -> bool;
```

```
    fn cpu_affinity(&self, worker_id: WorkerId) -> Option<usize>;
}
```

#### 然后： 

KCP/UDP Architecture V4 · High Performance Implementation 

```
struct ServerPolicy {
    // RSS / CPU / NIC Queue mapping
}
struct ClientPolicy {
    // least-load / adaptive parking / dynamic workers
}
```

#### 但这里有一个更重要的地方： 不要为了“代码共享”把 **Server** 和 **Client** 的 **packet ingress** 强行 抽象成完全相同。 

因为两者最大的性能区别其实在这里。 

#### Server： 

```
NIC
 │
 ▼
UDP Socket Worker0 ── SessionMap0
UDP Socket Worker1 ── SessionMap1
UDP Socket Worker2 ── SessionMap2
UDP Socket Worker3 ── SessionMap3
       ↑
 RSS / SO_REUSEPORT
```

#### Client 更可能是： 

```
             connect()
                │
         ClientPolicy
                │
       ┌────────┼────────┐
       ▼        ▼        ▼
      W0       W1       W2
       │        │        │
   Session   Session   Session
```

#### 因此我更推荐把整个库拆成 三层共享程度 ： 

```
┌───────────────────────────────────────┐
│ Public API                            │
│ KcpListener / KcpStream / connect     │
└───────────────────────────────────────┘
                    │
┌───────────────────────────────────────┐
│ Policy Layer                          │
│                                       │
│ ServerPolicy       ClientPolicy       │
│ RSS/reuseport      LeastLoad          │
│ fixed workers      dynamic workers    │
│ spin               adaptive park      │
└───────────────────────────────────────┘
                    │
┌───────────────────────────────────────┐
│ Shared Data Plane Core                │
│                                       │
│ Worker                                │
```

KCP/UDP Architecture V4 · High Performance Implementation 

```
│ Session                               │
│ SessionTable                          │
│ KCP Driver                            │
│ TimerWheel                            │
│ PacketPool                            │
│ SPSC App Channel                      │
│ Batch RX/TX                           │
│ Metrics                               │
└───────────────────────────────────────┘
```

#### 这里 **80%~90%** 的高性能代码都可以共享 。 

#### 尤其 `Session` 最好完全不要知道自己运行在 Server 还是 Client： 

```
struct Session {
    key: SessionKey,
    kcp: Kcp<KcpOutput>,
    app_rx: AppRxProducer,
    app_tx: AppTxConsumer,
    rx_waker: AtomicWaker,
    tx_waker: AtomicWaker,
    state: SessionState,
    next_update: Instant,
}
```

#### 这样 KCP hot path： 

```
UDP RX
 ↓
Session lookup
 ↓
kcp.input()
 ↓
kcp.recv()
 ↓
App RX Ring
 ↓
schedule next_update
```

#### Server / Client 完全一样 。 

区别只发生在： 

`Session` 从哪里来？ `↓ Server: packet → handshake → create_session() Client: connect() → create_session()` 

KCP/UDP Architecture V4 · High Performance Implementation 

以及： 

`Session` 放哪个 `Worker` ？ 

```
Server:
RSS / reuseport / packet ownership
```

`Client: hash + load` 还有一个我建议修改 **V4** 的地方 

#### 之前写的： 

Client 使用一致性 Hash + Least Load Correction 

我现在更倾向于 **Client** 不需要一致性哈希 。 

因为 Session 一旦创建就 Sticky： 

```
connect
   ↓
select_worker()
   ↓
Worker 3
   ↓
Session lifetime = Worker 3
```

之后根本不会再次计算 worker。 

#### 所以 Client 创建连接时直接： 

```
worker = power_of_two_choices();
```

#### 例如随机挑两个 Worker： 

```
let a = random_worker();
let b = random_worker();
if a.load() <= b.load() {
    a
} else {
    b
}
```

#### 会比： 

```
consistent hash
       +
scan all worker least-load
```

更便宜。 

KCP/UDP Architecture V4 · High Performance Implementation 

尤其 V3 当前实现这里： 

```
snap.workers
    .iter()
    .min_by_key(...)
```

实际上是 **O(N)** 。 

Worker 数少问题不大，但没有必要。 

所以最终我会设计成： 

```
pub struct WorkerPool<P> {
    workers: Box<[WorkerHandle]>,
    selector: P,
}
pub trait WorkerSelector {
    fn select(
        &self,
        workers: &[WorkerHandle],
        hint: SessionHint,
    ) -> usize;
}
```

#### 提供： 

`FixedHashSelector ↑ Server / RSS fallback PowerOfTwoSelector ↑ Client LeastLoadSelector ↑` 低连接数场景 `RoundRobinSelector ↑ benchmark / simple mode` 

#### 甚至 **IdlePolicy** 也不要和 **Selector** 绑在一起 ： 

```
WorkerPool
 ├── WorkerSelector
 ├── WorkerIdlePolicy
 ├── SocketStrategy
 ├── CpuAffinityPolicy
 └── WorkerScaler
```

#### 这样 Server： 

```
WorkerPoolConfig {
    selector: RssSelector,
```

KCP/UDP Architecture V4 · High Performance Implementation 

```
    idle: BusySpin,
    socket: PerWorkerReusePort,
    affinity: FixedCpu,
    scaler: Fixed,
}
```

Client： 

```
WorkerPoolConfig {
    selector: PowerOfTwo,
    idle: AdaptiveSpinPark,
    socket: PerWorker,
    affinity: OptionalAffinity,
    scaler: Dynamic,
}
```

底层还是完全同一个 **`WorkerPool`** 。 

这比维护 `ServerWorkerPool` / `ClientWorkerPool` 两套实现好很多，也方便 benchmark：你甚至可 以在不修改 Worker 核心代码的情况下直接比较 `BusySpin` 、 `AdaptivePark` 、 `PowerOfTwo` 、 `LeastLoad` 等策略。 

我认为这个结构比 V4 当前版本更合理，应该作为 **V4.1** 的一个核心修改 ： **WorkerPool** 统一， **Server/Client Policy** 化； **Worker** 、 **Session** 、 **Timer** 、 **PacketPool** 、 **KCP Driver 100%** 共用，只有 **Session establishment** 、 **Worker selection** 、 **Socket topology** 和 **Idle/Scaling** 策略不同。 

## 附录 **A** ：工程验收 **Gate** 

- 所有 SPSC 画出 producer/consumer，并能证明生命周期内各只有一个。 

- 连接 close/error 后，任何 Pending 的 read/write/flush/connect/accept 都能被唤醒。 

- 10 万 idle Session 时不允许 O(N) 每毫秒扫描。 

- App 不读时不能丢 KCP reliable payload；必须背压或断开。 

- 任何 packet path 不执行 heap allocation（稳态）。 

- Worker packet hot path 不调用 tokio::spawn/channel。 

- 每个 loop stage 有 budget，单 Session 不可无限占用 worker。 

- 生产环境配置记录 CPU affinity、IRQ/RSS/XPS、socket buffer、MTU。 

- 性能结论必须同时给 p99/p999 + CPU + context switch/cache miss，不只给平均值。 

KCP/UDP Architecture V4 · High Performance Implementation 

## 附录 **B** ：外部技术依据（实现时参考） 

Linux 内核文档：Scaling in the Linux Networking Stack（RSS/RPS/RFS/XPS）；Segmentation Offloads（UDP GSO）；Rust io-uring crate opcode 文档（RecvMsgMulti/RecvMultiBundle）；rtrb 文档（bounded SPSC ring buffer）。具体 API 版本应以项目锁定的 Cargo.lock 与目标 Linux kernel 版本为准。 

KCP/UDP Architecture V4 · High Performance Implementation 


